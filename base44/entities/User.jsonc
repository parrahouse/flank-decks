{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "username": {
      "type": "string",
      "description": "Unique display username chosen by the user, used for direct deck sharing and search"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}